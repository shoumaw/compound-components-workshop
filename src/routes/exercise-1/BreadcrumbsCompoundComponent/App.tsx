import React from "react";

const App: React.FC = () => (
  <div className="flex flex-col gap-y-4 font-bold">
    <h1>Breadcrumbs Compound Component</h1>
    {/*
      TODO:
      - Use the Breadcrumbs component to create a breadcrumb navigation.
      - Add multiple Breadcrumbs.Item components as children.
      - For each Breadcrumbs.Item, provide the 'href' prop except for the last item, which represents the current page and should have the 'isLast' prop.
    */}
  </div>
);

export default App;
